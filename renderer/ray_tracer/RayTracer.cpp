#include "RayTracer.h"

namespace renderer
{

void RayTracer::Render(const Scene & scene, uchar * imgBuffer)
{
    // TODO(Rodrigo Castiel).
}

void RayTracer::ParallelRender(const Scene & scene, uchar * imgBuffer, int numThreads)
{
    // TODO(Rodrigo Castiel).
}

void RayTracer::AdaptiveAntiAliasing(const Scene & scene, uchar * imgBuffer, int numThreads, int numSuperSample)
{
    // TODO(Rodrigo Castiel).
}

}  // namespace renderer.